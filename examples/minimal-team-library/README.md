# Minimal Team Skill Library

This example shows the smallest useful Skills Charter loop:

1. A candidate skill lives under `skills/release-notes/`.
2. A maintainer records a short evaluation report.
3. The skill can be promoted to `approved` after review.
4. The generated registry exposes install snippets only after approval.

Use it to explain the workflow in demos without introducing platform adapters or authentication.
